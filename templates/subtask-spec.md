# Subtask <n> — <slug>

> 自包含规格：执行者（子代理）只能依赖本文档与代码库，不依赖任何会话记忆。
> 属于任务：<task-id>　隔离：<独立 worktree / 任务 worktree 串行 / 只读>

## 背景（一句话）

<这个子任务在整体中的位置>

## 目标

<一句话>

## 验收

<!-- 每条附验证命令 -->

- [ ] <标准>（验证：<命令>）

## 涉及文件

- <路径>（<预期改动>）

## 约束与禁止

- 只做本子任务范围内的事，不顺手改其他代码
- 禁止改动：<明确列出不许碰的文件/模块>
- <其他约束：风格、依赖、兼容性>

## 产出要求

完成后报告：改动文件清单、验收命令运行结果、遗留问题。
